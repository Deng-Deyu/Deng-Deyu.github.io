# ─── 前端环境变量 (.env.local 本地开发用，不要提交到 Git) ────────────────────
# Cloudflare Worker API 地址
# 部署 Worker 后，在 Cloudflare Dashboard 看到的 URL，格式如下：
# https://turtlelet-api.YOUR-SUBDOMAIN.workers.dev
VITE_API_BASE=https://turtlelet-api.YOUR-SUBDOMAIN.workers.dev

# ─── 生产环境 ──────────────────────────────────────────────────────────────────
# 在 Cloudflare Pages 的 Dashboard → Settings → Environment Variables 里设置同样的变量
# VITE_API_BASE=https://turtlelet-api.YOUR-SUBDOMAIN.workers.dev
